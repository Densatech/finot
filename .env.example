VITE_API_URL=http://localhost:8000
# Optional: only needed if you want Vite's dev proxy to point somewhere else.
VITE_API_PROXY_TARGET=http://localhost:8000
# Optional: frontend fallback if the local backend is unavailable.
VITE_API_FALLBACK_URL=https://finot.onrender.com
